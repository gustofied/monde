#version 330 core

layout (location = 0) in vec3 aPos;



out vec4 vertexColor;

uniform float u_time;


void main(){
    gl_Position = vec4(aPos.x - sin(u_time), aPos.y - sin(u_time), aPos.z, 1.0);
    vertexColor = vec4(0.5, 1, 0.0, 1.0);
}